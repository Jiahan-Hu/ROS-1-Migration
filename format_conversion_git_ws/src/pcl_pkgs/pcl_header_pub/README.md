# pcl_header_pub

This package is to publish pcl header as a new topic. 

```
roslaunch pcl_header_pub launch_pcl_header_pub.launc
```

And it can also calculate the time difference between two headers of two point cloud. 
